# Aplicativo Stoq

Projeto organizado em duas partes:

- `back-end/`: API Java com Spring Boot
- `front-end/`: aplicativo Android

## Observações

Esta versão foi limpa para facilitar o envio ao GitHub:

- removida a pasta `.git` interna do back-end
- removida a pasta `target` do back-end
- removidos `.gradle`, `.idea`, `build` e `local.properties` do front-end
- adicionado `.gitignore` na raiz do projeto
